{
  "name": "school-crm-root",
  "version": "1.0.0",
  "description": "Root package for SchoolSaaS CRM",
  "scripts": {
    "build": "cd admin && npm install && npm run build",
    "start": "cd admin && npm start",
    "dev": "cd admin && npm run dev"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}